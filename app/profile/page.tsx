"use client";
import { useEffect,useState } from "react";
import Link from "next/link";
import { Account } from "@/lib/types";
export default function ProfilePage(){const [a,setA]=useState<Account|null>(null),[msg,setMsg]=useState("");useEffect(()=>{fetch('/api/me').then(r=>r.json()).then(setA)},[]);if(!a)return <main className="min-h-screen bg-ink text-goldSoft p-8">讀取會員資料中…</main>;async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/profile',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(a)});const d=await r.json();setMsg(r.ok?'會員資料已更新':d.error??'更新失敗')}return <main className="min-h-screen bg-ink px-4 py-8"><form onSubmit={save} className="max-w-xl mx-auto bg-parchment rounded-2xl border border-gold/50 p-6 sm:p-8"><div className="flex justify-between items-center"><h1 className="font-serifTC text-2xl text-lacquer font-bold">會員基本資料</h1><Link href="/" className="text-sm text-lacquer underline">返回廟門</Link></div><p className="text-sm text-ink/60 mt-1 mb-5">更新本人資料時，「本人」祈福對象也會同步更新。</p><div className="grid sm:grid-cols-2 gap-4"><label className="text-sm">姓名<input required value={a.real_name??''} onChange={e=>setA({...a,real_name:e.target.value})} className="mt-1 w-full rounded border px-3 py-2"/></label><label className="text-sm">LINE 名稱<input value={a.line_display_name??''} onChange={e=>setA({...a,line_display_name:e.target.value})} className="mt-1 w-full rounded border px-3 py-2"/></label><label className="text-sm sm:col-span-2">暱稱<input value={a.display_name??''} onChange={e=>setA({...a,display_name:e.target.value})} className="mt-1 w-full rounded border px-3 py-2"/></label></div><p className="text-sm mt-4 mb-1">農曆生日</p><div className="grid grid-cols-3 gap-2"><input required type="number" value={a.lunar_birth_year??''} onChange={e=>setA({...a,lunar_birth_year:Number(e.target.value)})} placeholder="年" className="rounded border px-3 py-2"/><input required type="number" min="1" max="12" value={a.lunar_birth_month??''} onChange={e=>setA({...a,lunar_birth_month:Number(e.target.value)})} placeholder="月" className="rounded border px-3 py-2"/><input required type="number" min="1" max="30" value={a.lunar_birth_day??''} onChange={e=>setA({...a,lunar_birth_day:Number(e.target.value)})} placeholder="日" className="rounded border px-3 py-2"/></div><label className="inline-flex gap-2 items-center mt-2 text-sm"><input type="checkbox" checked={Boolean(a.lunar_birth_leap_month)} onChange={e=>setA({...a,lunar_birth_leap_month:e.target.checked})}/>此月份為閏月</label><label className="block text-sm mt-4">祈福地址<textarea required rows={3} value={a.address??''} onChange={e=>setA({...a,address:e.target.value})} className="mt-1 w-full rounded border px-3 py-2"/></label>{msg&&<p className="mt-4 text-sm text-lacquer">{msg}</p>}<button className="mt-5 w-full bg-lacquer text-goldSoft font-bold rounded py-2.5">儲存會員資料</button></form></main>}
